/**
 * This code was GENERATED using the solita package.
 * Please DO NOT EDIT THIS FILE, instead rerun solita to update it or write a wrapper to add functionality.
 *
 * See: https://github.com/metaplex-foundation/solita
 */

import * as web3 from '@solana/web3.js'
import * as beetSolana from '@metaplex-foundation/beet-solana'
import * as beet from '@metaplex-foundation/beet'

/**
 * Arguments used to create {@link Challenger}
 * @category Accounts
 * @category generated
 */
export type ChallengerArgs = {
  authority: web3.PublicKey
  challengePda: web3.PublicKey
  triesRemaining: number
  redeemed: boolean
}
/**
 * Holds the data for the {@link Challenger} Account and provides de/serialization
 * functionality for that data
 *
 * @category Accounts
 * @category generated
 */
export class Challenger implements ChallengerArgs {
  private constructor(
    readonly authority: web3.PublicKey,
    readonly challengePda: web3.PublicKey,
    readonly triesRemaining: number,
    readonly redeemed: boolean
  ) {}

  /**
   * Creates a {@link Challenger} instance from the provided args.
   */
  static fromArgs(args: ChallengerArgs) {
    return new Challenger(
      args.authority,
      args.challengePda,
      args.triesRemaining,
      args.redeemed
    )
  }

  /**
   * Deserializes the {@link Challenger} from the data of the provided {@link web3.AccountInfo}.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static fromAccountInfo(
    accountInfo: web3.AccountInfo<Buffer>,
    offset = 0
  ): [Challenger, number] {
    return Challenger.deserialize(accountInfo.data, offset)
  }

  /**
   * Retrieves the account info from the provided address and deserializes
   * the {@link Challenger} from its data.
   *
   * @throws Error if no account info is found at the address or if deserialization fails
   */
  static async fromAccountAddress(
    connection: web3.Connection,
    address: web3.PublicKey,
    commitmentOrConfig?: web3.Commitment | web3.GetAccountInfoConfig
  ): Promise<Challenger> {
    const accountInfo = await connection.getAccountInfo(
      address,
      commitmentOrConfig
    )
    if (accountInfo == null) {
      throw new Error(`Unable to find Challenger account at ${address}`)
    }
    return Challenger.fromAccountInfo(accountInfo, 0)[0]
  }

  /**
   * Provides a {@link web3.Connection.getProgramAccounts} config builder,
   * to fetch accounts matching filters that can be specified via that builder.
   *
   * @param programId - the program that owns the accounts we are filtering
   */
  static gpaBuilder(
    programId: web3.PublicKey = new web3.PublicKey(
      'FFFFaFpoGXkYsidMpWTK6W2BeZ7FEfcYkg476zPFsLnS'
    )
  ) {
    return beetSolana.GpaBuilder.fromStruct(programId, challengerBeet)
  }

  /**
   * Deserializes the {@link Challenger} from the provided data Buffer.
   * @returns a tuple of the account data and the offset up to which the buffer was read to obtain it.
   */
  static deserialize(buf: Buffer, offset = 0): [Challenger, number] {
    return challengerBeet.deserialize(buf, offset)
  }

  /**
   * Serializes the {@link Challenger} into a Buffer.
   * @returns a tuple of the created Buffer and the offset up to which the buffer was written to store it.
   */
  serialize(): [Buffer, number] {
    return challengerBeet.serialize(this)
  }

  /**
   * Returns the byteSize of a {@link Buffer} holding the serialized data of
   * {@link Challenger}
   */
  static get byteSize() {
    return challengerBeet.byteSize
  }

  /**
   * Fetches the minimum balance needed to exempt an account holding
   * {@link Challenger} data from rent
   *
   * @param connection used to retrieve the rent exemption information
   */
  static async getMinimumBalanceForRentExemption(
    connection: web3.Connection,
    commitment?: web3.Commitment
  ): Promise<number> {
    return connection.getMinimumBalanceForRentExemption(
      Challenger.byteSize,
      commitment
    )
  }

  /**
   * Determines if the provided {@link Buffer} has the correct byte size to
   * hold {@link Challenger} data.
   */
  static hasCorrectByteSize(buf: Buffer, offset = 0) {
    return buf.byteLength - offset === Challenger.byteSize
  }

  /**
   * Returns a readable version of {@link Challenger} properties
   * and can be used to convert to JSON and/or logging
   */
  pretty() {
    return {
      authority: this.authority.toBase58(),
      challengePda: this.challengePda.toBase58(),
      triesRemaining: this.triesRemaining,
      redeemed: this.redeemed,
    }
  }
}

/**
 * @category Accounts
 * @category generated
 */
export const challengerBeet = new beet.BeetStruct<Challenger, ChallengerArgs>(
  [
    ['authority', beetSolana.publicKey],
    ['challengePda', beetSolana.publicKey],
    ['triesRemaining', beet.u8],
    ['redeemed', beet.bool],
  ],
  Challenger.fromArgs,
  'Challenger'
)
